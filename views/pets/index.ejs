<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pets</title>
</head>
<body>
  <h1>Pets</h1>
  <ul>
    <% pets.forEach(pet => { %>
    <li>
      <%= pet.text %> 
      - 
      <%= pet.hadPet ? 'had pet' : 'have Not had pet' %>
    </li>
    <% }) %>
  </ul>
</body>
</html>